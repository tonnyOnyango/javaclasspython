my_string = str("Hello World")                      # A string, which is a sequence of characters enclosed in quotes
my_int = int(9)                                     # An integer, representing whole numbers without decimals
my_float = float(89.0)                              # A float, representing numbers with decimal points
my_complex = 1j                                     # A complex number, represented as a real part and an imaginary part (1j means 0 + 1i)
my_bool = bool(True)                                # A boolean, which can be either True or False
my_bool2 = bool(False)                              # Another boolean, explicitly set to False
my_list = list(["John", "Erick", "Tonny"])          # A list, an ordered collection that can hold multiple items, defined using square brackets
my_tuple = tuple(("John", "Erick", "Tonny"))        # A tuple, similar to a list but immutable, defined using parentheses
my_set = set({"toyota", "mercedes", "nissan"} )     # A set, an unordered collection of unique items, defined using curly braces
my_dictionary = {"name": "Erick", "salary": 50000}  # A dictionary, a collection of key-value pairs, where keys are unique and used to access values

print(type(my_complex))
print(56>799)